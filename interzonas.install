<?
/**
 * Implements hook_field_schema().
 */
function interzonas_field_schema($field) {  
  // Return array that contains.
  return array(                  
    'columns' => array(           
      // In column there is a textf field.
      'campo_mapa' => array(          
        // Field has text type.
        'type' => 'text',       
      ), 
    ), 
  ); 
}
